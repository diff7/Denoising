tag: 'sound'
seed: 77
sample_len: 16384
data:
  shift: 2000
  dataset_train:
    file_path:  '/home/dev/other/denois/train_ss.txt'
    mode: 'train'

  dataset_val:
    file_path: '/home/dev/other/denois/val_ss.txt'
    mode: 'validation'

  loader_train:
    shuffle: True
    num_workers: 3
    batch_size: 32

model:
  n_layers: 12
  channels_interval:  24

loss:
  stft_sc_factor: 0.5
  stft_mag_factor: 0.5

optim:
  lr: 0.0001

trainer:
  sample_rate: 16000  #16000
  resume: 'epoch=59.ckpt'  # continue training resume_from
  epochs: 1200
  visualize_waveform_limit: 5
  visualize_audio_limit: 5
  visualize_spectrogram_limit: 5
  validation_interval: 5
  sample_length: 16384
  base_dir: '../results/'
  spectro_dir: 'speech'
  exp_name: 'unet_pl_ru_ukr_FFTLOSS_aug'
  cudnn_deterministic: True

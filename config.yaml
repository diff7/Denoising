tag: 'sound'
seed: 77
sample_len: 16384
data:
  dataset_train:
    file_path:  '/home/dev/other/denois/train_ss.txt'
    mode: 'train'

  dataset_val:
    file_path: '/home/dev/other/denois/val_ss.txt'
    mode: 'validation'

  loader_train:
    shuffle: True
    num_workers: 6
    batch_size: 32

model:
  n_layers: 12
  channels_interval:  24
  stft_sc_factor: 0.5
  stft_mag_factor: 0.5

optim:
  lr: 0.0001

loss: mse_loss # l1_lossw

trainer:
  sample_rate: 16000  #16000
  resume: None # continue training resume_from
  epochs: 1200
  visualize_waveform_limit: 5
  visualize_audio_limit: 5
  visualize_spectrogram_limit: 5
  validation_interval: 5
  sample_length: 16384
  base_dir: '../results/'
  spectro_dir: 'speech'
  exp_name: 'unet_pl_ru_ukr_no_white_noise'
  cudnn_deterministic: True
